/**
 * Frontend configuration file
 */

// API URL - defaults to localhost:8000 for development
export const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000';

// Other configuration variables can be added here
export const APP_NAME = 'Sizzle';
export const APP_DESCRIPTION = 'Search recipes by dish or ingredient, then cook them one step at a time.';
